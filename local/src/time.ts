export function logicalDate(instant: Date, timezone: string, boundaryHour: number): string { const shifted = new Date(instant.getTime() - boundaryHour * 3_600_000); return new Intl.DateTimeFormat("en-CA", { timeZone: timezone, year: "numeric", month: "2-digit", day: "2-digit" }).format(shifted); }
export function resolveDate(value: string, timezone: string, boundary: number): string { if (/^\d{4}-\d{2}-\d{2}$/.test(value)) return value; const now = new Date(); if (value === "today") return logicalDate(now, timezone, boundary); if (value === "yesterday") return logicalDate(new Date(now.getTime() - 86_400_000), timezone, boundary); throw new Error("CONFIG_ERROR: date must be today, yesterday, or YYYY-MM-DD"); }
